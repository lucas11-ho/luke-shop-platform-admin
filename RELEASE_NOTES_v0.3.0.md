# Release Notes v0.3.0 — Template Library + Readable Platform Controls

- Platform Template Library now includes readable template cards and typography preset catalog.
- Platform Owner can duplicate, enable/disable and inspect templates.
- Client module/limit/capability overrides use human-readable object controls rather than raw JSON.
- Audit details are readable, searchable, filterable, paginated and expandable without raw JSON blocks.
