# Technical Analysis v0.3.0

This release stays API-driven and does not bypass tenant isolation. Design choices are represented as controlled Experience Engine fields rather than arbitrary source/HTML/JavaScript. Raw JSON-first administration has been replaced with readable structured controls where applicable.

The iPhone/iOS font preset uses native system-family CSS names and fallback stacks. No Apple font binaries are included.
