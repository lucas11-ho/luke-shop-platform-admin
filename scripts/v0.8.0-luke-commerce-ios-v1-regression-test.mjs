import fs from'node:fs';
const source=fs.readFileSync('src/pages/ThemesPage.jsx','utf8');let n=0;const pass=(ok,msg)=>{if(!ok)throw new Error(`FAIL ${msg}`);n++;console.log(`PASS ${msg}`)};
pass(source.includes("key:'LUKE_COMMERCE_IOS'")&&/version:'1\.\d+\.0'/.test(source),'Built-in commercial package remains an immutable LUKE_COMMERCE_IOS semantic version');
pass(source.includes("name:'Luke Commerce iOS v1'"),'Package keeps the customer-facing commercial theme name');
pass(source.includes("typography:{preset:'IOS_SYSTEM',scale:'standard'") ,'Package keeps the real iOS system typography renderer as its default');
pass(source.includes("primary:'ios_filled'")&&source.includes("secondary:'ios_tonal'")&&source.includes("icon:'ios_circle'"),'Package preserves the commercial iOS control recipe');
pass(source.includes("mobile:'ios_tab'")&&source.includes("active_indicator:'filled_icon'")&&source.includes("container:'edge'"),'Default navigation remains the clean iOS tab recipe');
pass(source.includes("pack:'PHOSPHOR_NAV'")&&source.includes("home:'house'")&&source.includes("profile:'user-circle'"),'Professional Phosphor icon family and defaults remain bundled');
pass(source.includes("product_card:'minimal'")&&source.includes("form_control:'ios_grouped'"),'Commercial Product Card and form defaults remain renderer-backed');
pass(source.includes('stores stay unchanged'),'new package versions do not silently mutate stores pinned to earlier exact versions');
pass(source.includes('Load built-in package')&&source.includes('Install as Draft'),'Platform Owner can load the built-in package without bypassing Draft review');
pass(source.includes('Published versions are immutable'),'Installer preserves immutable-version guidance');
pass(!source.includes('dangerouslySetInnerHTML')&&!source.includes('eval(')&&!source.includes('new Function'),'Theme Library executes no theme package code');
console.log(`${n}/${n} Luke Commerce iOS v1 Platform checks passed`);