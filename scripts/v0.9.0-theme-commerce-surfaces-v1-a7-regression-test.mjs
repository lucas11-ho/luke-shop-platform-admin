import fs from'node:fs';
const source=fs.readFileSync('src/pages/ThemesPage.jsx','utf8');let n=0;const pass=(ok,msg)=>{if(!ok)throw new Error(`FAIL ${msg}`);n++;console.log(`PASS ${msg}`)};
pass(source.includes("key:'LUKE_COMMERCE_IOS'")&&/version:'1\.\d+\.0'/.test(source),'current Luke Commerce package remains an immutable semantic version');
pass(source.includes("header_surface:['standard','ios_clean','compact','glass']"),'later packages preserve bounded Header surfaces');
pass(source.includes("search_surface:['standard','ios_search','pill','sheet']"),'later packages preserve bounded Search surfaces');
pass(source.includes("account_surface:['standard','ios_grouped','soft','compact']"),'later packages preserve bounded Account surfaces');
pass(source.includes("cart_surface:['standard','ios_grouped','soft','compact']")&&source.includes("checkout_surface:['standard','ios_grouped','soft','compact']"),'later packages preserve bounded Cart and Checkout surfaces');
pass(source.includes("header_surface:'ios_clean',search_surface:'ios_search',account_surface:'ios_grouped',cart_surface:'ios_grouped',checkout_surface:'ios_grouped'"),'later packages preserve renderer-backed commercial defaults for all five surfaces');
pass(source.includes('Commerce Surface Composers')||source.includes('Commerce Surface Composer'),'Platform continues to describe the A7 governed commerce-surface capability');
pass(source.includes('Store Designer structure and required commerce flows'),'later package preview preserves Store Designer and commerce authority separation');
pass(source.includes('stores stay unchanged'),'later package versions explicitly preserve earlier exact store selections');
pass(source.includes('Install it as a Draft, review it, then publish immutable'),'Platform Owner still controls manual Draft review and publish lifecycle');
pass(source.includes('renderer-backed'),'installer continues to document renderer-backed named variants only');
pass(source.includes('arbitrary CSS/SVG/HTML/JavaScript or custom font URLs are not accepted'),'installer keeps non-executable package safety boundary');
pass(!source.includes('dangerouslySetInnerHTML')&&!source.includes('eval(')&&!source.includes('new Function'),'A7 package path executes no package code');
console.log(`${n}/${n} Luke Theme Commerce Surfaces v1 A7 Platform checks passed`);