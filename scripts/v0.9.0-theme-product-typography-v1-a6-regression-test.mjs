import fs from'node:fs';
const source=fs.readFileSync('src/pages/ThemesPage.jsx','utf8');let n=0;const pass=(ok,msg)=>{if(!ok)throw new Error(`FAIL ${msg}`);n++;console.log(`PASS ${msg}`)};
pass(source.includes("key:'LUKE_COMMERCE_IOS'")&&/version:'1\.\d+\.0'/.test(source),'current Luke Commerce package remains an immutable semantic version');
pass(source.includes("product_image_ratio:['square','portrait','landscape','auto']"),'later packages preserve bounded Product Card image ratios');
pass(source.includes("product_badge_position:['top_left','top_right','inline','hidden']"),'later packages preserve bounded Product Card badge positions');
pass(source.includes("product_price_layout:['stacked','inline','emphasis','compact']"),'later packages preserve bounded Product Card price layouts');
pass(source.includes("product_quick_add:['hidden','button','icon']"),'later packages preserve bounded quick-add presentation');
pass(source.includes("product_density:['compact','comfortable','spacious']")&&source.includes("product_elevation:['flat','soft','raised']"),'later packages preserve bounded density and elevation');
pass(source.includes("typography_preset:['ios_system','system_minimal','modern_sans','clean_commerce'") ,'later packages preserve approved renderer font presets');
pass(source.includes("typography_heading_weight:['regular','semibold','bold','heavy']")&&source.includes("typography_body_weight:['regular','medium','semibold']"),'later packages preserve bounded typography role weights');
pass(source.includes("typography_line_height:['tight','standard','relaxed']")&&source.includes("typography_letter_spacing:['tight','normal','wide']"),'later packages preserve bounded typography rhythm');
pass(source.includes("product_card:'minimal',product_image_ratio:'square'")&&source.includes("typography_preset:'ios_system'"),'later packages preserve renderer-backed Product Card and typography defaults');
pass(source.includes("typography:{preset:'IOS_SYSTEM',scale:'standard',heading_weight:'semibold'") ,'later package typography manifest preserves bounded role defaults');
pass(source.includes('Product Card')&&source.includes('Typography'),'Platform continues to expose the complete A6 governed composer surface');
pass(source.includes('stores stay unchanged'),'later package versions explicitly preserve earlier exact store selections');
pass(source.includes('Install it as a Draft, review it, then publish the immutable'),'Platform Owner still controls install/review/publish lifecycle');
pass(source.includes('custom font URLs are not accepted'),'installer rejects arbitrary font/CSS authority');
pass(!source.includes('dangerouslySetInnerHTML')&&!source.includes('eval(')&&!source.includes('new Function'),'A6 package path executes no package code');
console.log(`${n}/${n} Luke Theme Product + Typography Composer v1 A6 Platform checks passed`);