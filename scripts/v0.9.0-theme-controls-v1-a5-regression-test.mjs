import fs from'node:fs';
const read=p=>fs.readFileSync(p,'utf8');let n=0;const pass=(ok,msg)=>{if(!ok)throw new Error(`FAIL ${msg}`);n++;console.log(`PASS ${msg}`)};
const page=read('src/pages/ThemesPage.jsx');
pass(page.includes("key:'LUKE_COMMERCE_IOS'")&&/version:'1\.\d+\.0'/.test(page),'current Luke Commerce package remains an immutable semantic version');
pass(page.includes("button_primary:['ios_filled','ios_tonal','ios_outline','ios_soft','ios_pill']"),'later versions preserve bounded primary button recipes');
pass(page.includes("button_secondary:['ios_tonal','ios_outline','ios_plain','ios_soft']"),'later versions preserve bounded secondary button recipes');
pass(page.includes("button_destructive:['ios_destructive','ios_destructive_soft','ios_destructive_outline']"),'later versions preserve bounded destructive recipes');
pass(page.includes("button_icon:['ios_circle','ios_square','ios_plain']")&&page.includes("button_size:['compact','standard','large']"),'later versions preserve icon and size button choices');
pass(page.includes("form_control:['ios_grouped','soft_filled','outline','minimal']"),'later versions preserve bounded input appearances');
pass(page.includes("form_size:['compact','standard','large']")&&page.includes("form_group:['inset_grouped','card','flat']"),'later versions preserve form sizing and grouping');
pass(page.includes("form_control:'ios_grouped'")&&page.includes("form_size:'standard'")&&page.includes("form_group:'inset_grouped'"),'later versions preserve safe form defaults');
pass(page.includes('Button')&&page.includes('Forms/Input'),'Platform still describes the governed A5 composer surface');
pass(page.includes('stores stay unchanged'),'Platform communicates immutable no-auto-upgrade behavior');
pass(page.includes('Install as Draft')&&page.includes('Published versions are immutable'),'manual Draft review and immutable publish lifecycle remains');
pass(!page.includes('dangerouslySetInnerHTML')&&!page.includes('eval(')&&!page.includes('new Function'),'built-in package path executes no package code');
console.log(`${n}/${n} Luke Theme Controls v1 A5 Platform checks passed`);