import React,{useEffect}from'react';export const tone=s=>['ACTIVE','PUBLISHED'].includes(String(s).toUpperCase())?'success':['SUSPENDED','DRAFT'].includes(String(s).toUpperCase())?'warning':['DISABLED','INACTIVE'].includes(String(s).toUpperCase())?'danger':'neutral';export function Badge({children,tone:t='neutral'}){return <span className={`badge ${t}`}>{children}</span>}export function PageHeader({eyebrow='Platform Control Center',title,description,actions}){return <div className="page-header"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1>{description&&<p>{description}</p>}</div>{actions&&<div className="actions">{actions}</div>}</div>}export function Card({title,subtitle,children,className=''}){return <section className={`card ${className}`}><div className="card-head"><div><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div></div>{children}</section>}export function Stat({label,value,detail}){return <div className="stat"><span>{label}</span><strong>{value??'—'}</strong>{detail&&<small>{detail}</small>}</div>}export function Field({label,hint,children}){return <label className="field"><span>{label}</span>{children}{hint&&<small>{hint}</small>}</label>}export function ErrorBox({error}){return error?<div className="alert error"><strong>{error.code||'Request failed'}</strong><span>{error.message}</span>{error.requestId&&<small>Request: {error.requestId}</small>}</div>:null}export function Loading(){return <div className="loading">Loading…</div>}export function Empty({title='Nothing here yet',body='No records to show.'}){return <div className="empty"><strong>{title}</strong><span>{body}</span></div>}export function Modal({open,onClose,title,children,footer,size='md'}){if(!open)return null;return <div className="modal-bg" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className={`modal ${size}`}><div className="modal-head"><h2>{title}</h2><button className="icon" onClick={onClose}>×</button></div><div className="modal-body">{children}</div>{footer&&<div className="modal-foot">{footer}</div>}</div></div>}export function Toast({message,onDone}){useEffect(()=>{if(!message)return;const t=setTimeout(onDone,3000);return()=>clearTimeout(t)},[message,onDone]);return message?<div className="toast">{message}</div>:null}export function humanizeKey(value=''){return String(value).replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase())}export function FriendlyObject({value}){return <div className="friendly-object">{Object.entries(value||{}).map(([k,v])=><div key={k}><span>{humanizeKey(k)}</span><strong>{typeof v==='boolean'?(v?'Enabled':'Disabled'):typeof v==='object'?Object.entries(v||{}).map(([a,b])=>`${humanizeKey(a)}: ${String(b)}`).join(' · '):String(v)}</strong></div>)}</div>}export function ObjectEditor({label,value,onChange}){return <Field label={label}><div className="object-editor">{Object.entries(value||{}).map(([k,v])=><label key={k}><span>{humanizeKey(k)}</span>{typeof v==='boolean'?<input type="checkbox" checked={v} onChange={e=>onChange({...value,[k]:e.target.checked})}/>:typeof v==='number'?<input type="number" value={v} onChange={e=>onChange({...value,[k]:Number(e.target.value)})}/>:<input value={String(v??'')} onChange={e=>onChange({...value,[k]:e.target.value})}/>}</label>)}</div></Field>}

export function Tabs({value,onChange,items}){return <div className="tabs">{items.map(i=><button key={i.value} className={value===i.value?'active':''} onClick={()=>onChange(i.value)}>{i.label}{i.count!==undefined&&<span>{i.count}</span>}</button>)}</div>}
