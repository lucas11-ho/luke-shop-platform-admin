import React,{useState}from'react';import{useAuth}from'../auth/AuthContext.jsx';import{ErrorBox,Field}from'../components/UI.jsx';export function LoginPage(){const{login}=useAuth();const[form,setForm]=useState({email:'',password:''}),[err,setErr]=useState(null),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setErr(null);try{await login(form);location.hash='#/dashboard'}catch(x){setErr(x)}finally{setBusy(false)}};return <div className="login platform-login"><div className="login-copy"><div className="brand big"><span>L</span><div><strong>Luke Shop</strong><small>Platform Control Center</small></div></div><div className="login-kicker">Multi-tenant commerce infrastructure</div><h1>Operate every client business from one protected control plane.</h1><p>Provision tenants, shape commercial plans, control storefront capabilities and oversee the platform without crossing into a merchant workspace.</p><div className="trust-grid"><div><strong>Tenant isolation</strong><span>Independent client boundaries</span></div><div><strong>Owner-only access</strong><span>Separate platform identity</span></div><div><strong>Audit first</strong><span>Every sensitive action traceable</span></div></div></div><div className="login-form-wrap"><form onSubmit={submit}><div className="login-lock">L</div><div className="eyebrow">Platform owner access</div><h2>Welcome back</h2><p>Sign in with your Luke Platform Owner account. Client Admin credentials are intentionally rejected here.</p><ErrorBox error={err}/><Field label="Email address"><input type="email" required autoComplete="username" placeholder="owner@company.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Password"><input type="password" required minLength="12" autoComplete="current-password" placeholder="••••••••••••" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field><button className="primary" disabled={busy}>{busy?'Opening control center…':'Open control center'}</button><div className="secure-note"><span>●</span> Secure platform-only session</div></form></div></div>}
