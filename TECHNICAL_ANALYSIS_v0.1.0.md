# Technical Analysis

This frontend is deliberately separate from Client Admin. It consumes only Backend v0.7.0 Platform Control Plane APIs. Tenant isolation remains enforced by the backend; the Platform Admin does not reuse merchant credentials or tenant-scoped sessions.
